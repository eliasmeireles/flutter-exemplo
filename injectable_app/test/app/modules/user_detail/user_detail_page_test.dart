import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:injectable_app/app/modules/user_detail/user_detail_page.dart';

void main() {
  testWidgets('UserDetailPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UserDetailPage(title: 'UserDetail')));
    //  final titleFinder = find.text('UserDetail');
    //  expect(titleFinder, findsOneWidget);
  });
}
