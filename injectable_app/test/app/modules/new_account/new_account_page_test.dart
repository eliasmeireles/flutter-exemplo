import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:injectable_app/app/modules/new_account/new_account_page.dart';

void main() {
  testWidgets('NewAccountPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewAccountPage(title: 'NewAccount')));
    //  final titleFinder = find.text('NewAccount');
    //  expect(titleFinder, findsOneWidget);
  });
}
